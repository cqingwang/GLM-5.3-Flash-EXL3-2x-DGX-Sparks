# GLM-5.3 Flash EXL3 启动修复计划

## 范围

- 修复 `spark/deploy.sh` 从 `spark/config.yaml` 生成 GLM `.env` 时的长上下文上限。
- 以本地仓库为唯一配置源，同步到 `spark-a` 后重启并验证双机服务。
- 不修改模型权重、不复制容器内权重、不调整 NCCL/网络或无关引擎配置。

## 根因

- 远端容器实际收到 `MAX_MODEL_LEN=983040`、`MAX_NUM_SEQS=6`、`MAX_NUM_BATCHED_TOKENS=8192`。
- head 的 vLLM 在 KV cache 初始化时明确报错：`983040` 需要 `15.2 GiB`，可用 KV 只有 `14.91 GiB`，最大可估算长度为 `942592`。
- `glm53_flash_exl3/start.sh` 的安全默认值已经是 `850000`，但 `deploy.sh` 会将 `config.yaml` 的 `max_token` 写入 `.env`，因此配置中的 `983040` 覆盖了脚本默认值。

## 修复

- 将 `spark/config.yaml` 中 GLM 引擎的 `max_token` 设为已验证的 `850000`。
- 增加回归测试，验证模型查询链路输出的 `max_token` 也是 `850000`，防止只改脚本默认值而总控再次覆盖。

## 验证门禁

1. 修复前运行新增回归测试，确认当前 `983040` 配置使断言失败。
2. 修复后运行 `python3 spark/tests/test_model_path_contract.py`、`bash -n spark/deploy.sh spark/glm53_flash_exl3/start.sh` 和 `git diff --check`。
3. 只同步 `spark/config.yaml` 与回归测试到 `spark-a` 的 `/opt/spark`；远端启动入口再由 `deploy.sh` 生成 `.env`。
4. 远端验证容器实际 `MAX_MODEL_LEN=850000`、双节点容器均存活、head `/health` 与 `/v1/models` 返回 200，并确认 `/models` 仍为只读挂载。

