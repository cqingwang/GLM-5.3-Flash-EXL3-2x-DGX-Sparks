# GLM-5.3 Flash EXL3 TP4 启动链路修复计划

## 范围

- 修复 `start-tp4.sh` 的 direct 预加载模型与 DFlash2 资产校验，确保缺失权重在启动前失败。
- 修复 TP4 的四节点生命周期入口，使父级 `stop.sh` 能停止 head 与三个 worker。
- 保持 TP2 `start.sh`、TP4 NCCL/网络拓扑和容器内 vLLM 参数不变。
- 只修改启动链路和对应回归测试，不触碰模型权重。

## 根因

- TP4 的 direct 分支在 `SKIP_DOWNLOAD=1` 下只验证 `config.json`，没有验证目标模型和 DFlash2 的 safetensors。
- `preflight()` 和 `sync_weights()` 对 worker 也只验证配置文件，缺权重会延迟到容器启动后才暴露。
- `config.yaml` 的 GLM TP4 引擎使用 `stop.sh`，但 `stop.sh` 没有 TP4 的检测、参数分支或 rank 2/3 清理逻辑。

## 修复

- 增加 TP4 本地与远端 direct asset guard：`config.json` + 至少一个 safetensors。
- 让 `download`、`start`、`preflight`、`sync_weights` 共用同一资产契约，`SKIP_DOWNLOAD/SKIP_SYNC` 只跳过传输，不跳过完整性检查。
- 为 `stop.sh` 增加 TP4 head 检测、`tp4|4` 参数和 `all` 分支，并继续复用 `start-tp4.sh stop` 的逐 rank 清理逻辑。
- 增加 CPU-only 回归测试，覆盖 direct 缺权重失败和 TP4 stop dispatch。

## 验证门禁

1. 修复前运行新增 TP4 回归测试，确认 direct 缺权重和 `stop.sh tp4` 均失败。
2. 修复后运行新增测试、`pytest` 既有 TP4/配置测试、全部 shell `bash -n` 和 `git diff --check`。
3. 本地只能证明生成命令、资产门禁和生命周期分发；未连接四台 Spark 时不得宣称 NCCL、Docker、GPU、`/health` 已通过。
4. 真实验收需在四节点上确认 head/rank1/rank2/rank3 容器、`/health`、`/v1/models`、NCCL rank/world-size 和 DFlash2 warmup。

## 2026-09-18 性能验收与配置修复

### 现象与证据

- 官方两台 Prose 基线是 x1 约 36.1/37.1、x2 约 25.0/51.1、x4 约 19.4/75.3 tok/s；当前四节点实测 x1 36.6、x2 25.2/48.9、x4 19.6/76.8，量级一致。
- 受控 API 基线在当前远端服务上以真实 `completion_tokens` 计数，x1 中位 38.24 tok/s，x4 聚合中位约 84.07 tok/s；没有证据表明 TP4 发生了官方功能回退。
- 远端 `.env.tp4` 仍被总控写成通用 `MAX_NUM_SEQS=6`、`MAX_NUM_BATCHED_TOKENS=8192`，且旧文件保留 `DFLASH_TOKENS=7`；TP4 模板的实测值 `8/2048/k3/fair` 没有成为运行时配置。

### 修复边界

- 将 TP4 实测参数收敛到 `config.yaml` 的 engine 节点，由 `program.py query-model` 输出并由 `deploy.sh` 写入托管块，解决旧 `.env.tp4` 和 common 配置覆盖问题。
- 本轮不直接打开 TP2 专用的 adaptive-k、dense-FP8 或 cooperative overlay；它们不在 TP4 启动器中，且需要独立的数值一致性和吞吐 A/B。

### 验证

- 先以静态回归测试证明修复前缺失 TP4 字段，后以 pytest、shell 语法和 `git diff --check` 证明本地生成链路。
- 远端重启后核对四节点环境实际值、rank/world-size、服务健康、warmup，以及 x1/x4 的 `completion_tokens` 吞吐；若单流仍约 36 tok/s，这是 TP4 同步拓扑的预期而非功能破坏。

## 2026-09-18 官方镜像失败边界与环路同步修复

### 新证据

- 官方 `:exl3-instanttensor` 已完成四节点镜像一致性检查，但当前 checkpoint 在 `instanttensor.safe_open` 元数据读取阶段报 `NoneType` 解包异常，随后 rank3 segfault；因此不能把该 tag 直接当作当前权重的可用性能基线。
- 原先已在相同权重上通过 TP4/API 的 wheel-less `:exl3` 镜像四机均存在，恢复它比继续重复拉取官方镜像更符合本地代码为源和可复现验收要求。
- 镜像传输采用 A→C→D→B 的管理网环路；loader 必须一次性保存 `PIPESTATUS`，并为嵌套 SSH 接受新 host key。

### 决策与门禁

- `config.yaml` 作为唯一来源写出 `image/load_format/skip_pull/skip_build/ship_image_ring`，避免 `.env` 残留覆盖 TP4 profile。
- 恢复后必须依次通过：四机镜像 ID/大小一致、rank/world-size/NCCL、patch hash、`/health`、认证 `/v1/models`、官方 sparkDash Code prompt 的 1/2/4 并发 `completion_tokens` benchmark。
- InstantTensor 是源码默认的官方 TP4 profile；重新部署前必须在隔离 profile 中复现并修复 safe_open 元数据失败，不能只验证 `docker pull` 或容器进程存在。wheel-less `:exl3` 仅作为显式恢复服务 fallback，不得作为官方 TP4 性能结论。

### 最终运行态结果

- 四机容器 image ID 全部为 `sha256:92f831b6f56909c1586dbd9b54e078a2a7aaebcbc7533d3381dc6022904b3f33`；四机环境一致为 `TP=4/NNODES=4/LOAD_FORMAT=/DFLASH_TOKENS=3/DFLASH_DRAFT_TP=4/GPU_MEM_UTIL=0.75/MAX_NUM_SEQS=8/MAX_NUM_BATCHED_TOKENS=2048/ENFORCE_EAGER=1/slice=64`。
- `world_size=4`、NCCL 2.30.7、sparse MLA patch hash `f1854c0c...`、`/health=200`、认证 `/v1/models` 和 post-ready warmup `24/24` 均通过。
- sparkDash Code 三次中位数：x1 `54.70`，x2 `86.08/43.20`，x4 `173.29/43.32` tok/s（aggregate/stream）；全部请求均返回 512 completion tokens。
