# GLM-5.3 Flash EXL3 双节点最新镜像部署计划

## 范围

- 以本地 `spark/glm53_flash_exl3/README.md`、`start.sh` 和父目录 `spark/config.yaml` 为唯一源。
- 保持现有模型 ID/路径 `Mia-AiLab/GLM-5.3-Flash-exl3-4bpw-ablit`、DFlash2 路径、`spark-a/spark-b` 节点、TP=2/NNODES=2 不变。
- 仅对齐最新 GHCR InstantTensor 镜像、`LOAD_FORMAT`、镜像拉取策略和 README 的 served model alias；不改模型文件。

## 生效链路

`spark/config.yaml` → `spark/program.py query-model` → `spark/deploy.sh configure_env` →
`glm53_flash_exl3/.env` → `start.sh` → 两节点 Docker 容器。

## 目标参数

- 镜像：`ghcr.io/miaai-lab/glm-5.3-flash-2x-dgx-sparks:exl3-instanttensor`
- 加载：`LOAD_FORMAT=instanttensor`，`SKIP_PULL=0`，`SKIP_BUILD=1`
- 拓扑：TP=2、NNODES=2、DFlash2 k=7、CUDA graphs、E3 grouped MoE
- README 配方：850000 context、GPU_MEM_UTIL=0.85、MAX_NUM_SEQS=4、MNBT=7168、KV cap 14 GiB、adaptive-k `ema`、dense FP8 `dense,kda`
- API served model：`GLM-5.3-Flash-EXL3`；底层模型 ID 仍为现有 ablit checkpoint。

## 验证门禁

1. 本地：YAML/路由解析、shell 语法、Python 编译、配置合同测试、`git diff --check`。
2. 远端部署：两端镜像 digest/容器、实际只读模型挂载、启动参数和关键日志；不以命令退出码单独判定。
3. 服务：`/health`、认证 `/v1/models`、标准 smoke。
4. 性能：按 README 固定 high-accept Structured/Code 合同，`temperature=0`、`top_p=1`、thinking off、`max_tokens=400`，对 ×1/×2/×4 分别重复测量并记录 TTFT、Stream tok/s、Aggregate tok/s；与 README 62.9/51.7/37.1 和 62.9/103.3/146.5 对照，明确标注冷启动/热服务与偏差。

## 风险与止损

- InstantTensor 对当前 ablit checkpoint 若仍在 safe_open 元数据阶段失败，保留原文件不动，记录完整日志并停止，不盲目切换模型或节点。
- 现有未提交改动不回滚；远端只单向接收本地明确范围内的源码/config，不从远端反向覆盖本地。
